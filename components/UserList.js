// import React from 'react';
// import {Button, View, Text, SectionList, StyleSheet, StatusBar} from "react-native";
// import UserItem from "./UserItem";

// const UserList = ({users, first_name, remove}) => {
//   return (
//     <View>
//       <Text>{first_name}</Text>
//       {users.map((user, index) =>
//       <UserItem remove={remove} number={index+1} user={user} keyExtractor={(item) => item.id}></UserItem>
//       )}
//     </View>
//   );
// };

// export default UserList;

// <div className={'posts-container'}>
    //   <h2 className={'posts-title'}>{first_name}</h2>
    //   <ul className={'posts-list'}>
    //     {users.map((user, index)=>
    //       <UserItem remove={remove} number={index + 1} user={user} key={user.id}/>
    //     )}
    //   </ul>
    // </div>