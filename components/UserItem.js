// import React from 'react';
// import {Button, View, Text, SectionList, StyleSheet, StatusBar} from "react-native";
// import MyButton from "./UI/button/MyButton";

// const UserItem = ({user, first_name, remove}) => {
//   return (
//     <View>
//       <Text>{first_name}</Text>
//       <Text>{user.first_name}</Text>
//       <Text>{user.email}</Text>
//       <MyButton onPress={() => remove(user)}>remove</MyButton>
//     </View>
//   );
// };

// export default UserItem;

{/* <li className={'post-item'}>
      <div style={{display: 'flex'}}>
        <div className={'post-item-id'}>
          <p>{first_name}</p>
        </div>
        <div className={'post-item-content'}>
          <h1>{user.first_name}</h1>
          <p>{user.email}</p>
        </div>
      </div>
      <div>
        <MyButton onClick={()=> remove(user)}>remove</MyButton>
      </div>
    </li> */}